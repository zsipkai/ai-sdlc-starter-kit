# Claude Code Entry Point

@AGENTS.md

Claude Code must treat `AGENTS.md` as this repository's single canonical agent constitution. This adapter may contain harness-specific loading notes, but it must not duplicate or override project rules, lifecycle states, quality gates, or human-approval requirements.
